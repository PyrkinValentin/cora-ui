import { MeterRoot, MeterLabel, MeterValue, MeterTrack, MeterIndicator } from "./meter"

export const Meter = {
	Root: MeterRoot,
	Label: MeterLabel,
	Value: MeterValue,
	Track: MeterTrack,
	Indicator: MeterIndicator,
} as const